/**
 * Placeholder so "optional" group runs successfully when no other optional tests exist.
 * Remove this file when you add real optional tests.
 */
import { describe, it, expect } from "vitest";

describe("optional (placeholder)", () => {
  it("optional group is executable", () => {
    expect(true).toBe(true);
  });
});
