<UserControl x:Class="WinPodiums.Plugin.UI.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="400">
  <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12,8">
    <StackPanel>
      <TextBlock Text="WinPodiums" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
      <TextBlock Text="Status" FontWeight="SemiBold" Margin="0,0,0,4"/>
      <TextBlock x:Name="StatusText" Text="Not linked" Margin="0,0,0,12" TextWrapping="Wrap"/>
      <TextBlock Text="API base URL" FontWeight="SemiBold" Margin="0,0,0,4"/>
      <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="ApiUrlBox" Grid.Column="0" Margin="0,0,8,0" Padding="6,4" VerticalContentAlignment="Center"/>
        <Button x:Name="SaveUrlButton" Grid.Column="1" Content="Save" Padding="12,4" Click="SaveUrlButton_Click"/>
      </Grid>
      <TextBlock Text="Authentication" FontWeight="SemiBold" Margin="0,8,0,4"/>
      <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
        <Button x:Name="LinkDiscordButton" Content="Link to Discord" Padding="12,6" Margin="0,0,8,0" Click="LinkDiscordButton_Click"/>
        <Button x:Name="LogoutButton" Content="Log out" Padding="12,6" Click="LogoutButton_Click"/>
      </StackPanel>
      <TextBlock Text="Manual token (optional)" Margin="0,0,0,4"/>
      <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="TokenBox" Grid.Column="0" Margin="0,0,8,0" Padding="6,4" VerticalContentAlignment="Center" MaxLength="8"/>
        <Button x:Name="PasteTokenButton" Grid.Column="1" Content="Paste token" Padding="12,4" Click="PasteTokenButton_Click"/>
      </Grid>
      <TextBlock Text="Heartbeat" FontWeight="SemiBold" Margin="0,8,0,4"/>
      <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
        <Button x:Name="HeartbeatButton" Content="Send heartbeat" Padding="12,6" Margin="0,0,8,0" Click="HeartbeatButton_Click"/>
        <TextBlock x:Name="HeartbeatStatus" Text="" VerticalAlignment="Center" Foreground="Gray"/>
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl>
